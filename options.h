#include <stdbool.h>
#include <sys/stat.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <dirent.h>
#include <string.h>
#include <sys/types.h>
#include <fts.h>
#include <pwd.h>
#include <grp.h>
#include <time.h>
#include <ctype.h>
#include <limits.h>
#include <errno.h>
#include <math.h>

extern bool A_FLAG;
extern bool a_FLAG;
extern bool i_FLAG;
extern bool F_FLAG;
extern bool S_FLAG;
extern bool t_FLAG;
extern bool u_FLAG;
extern bool r_FLAG;
extern bool c_FLAG;
extern bool l_FLAG;
extern bool f_FLAG;
extern bool h_FLAG;
extern bool k_FLAG;
extern bool n_FLAG;
extern bool q_FLAG;
extern bool R_FLAG;
extern bool s_FLAG;
extern bool w_FLAG;
extern bool d_FLAG;